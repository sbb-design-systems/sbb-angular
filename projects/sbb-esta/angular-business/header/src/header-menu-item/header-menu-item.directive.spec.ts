import { HeaderMenuItemDirective } from './header-menu-item.directive';

describe('HeaderMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderMenuItemDirective();
    expect(directive).toBeTruthy();
  });
});
