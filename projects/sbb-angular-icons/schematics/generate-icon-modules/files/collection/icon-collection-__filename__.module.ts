/*
 * Autogenerated by schematics
 */
import { NgModule } from '@angular/core';
<% for (let collection of collections) { %>
import { IconCollection<%= classify(collection.name) %>Module } from './<%= dasherize(collection.name) %>/icon-collection-<%= dasherize(collection.name) %>.module';<% } %>
<% for (let icon of icons) { %>
import { Icon<%= classify(icon.name) %>Module } from './icon-<%= dasherize(icon.name) %>.module';<% } %>

const modules = [<% for (let collection of collections) { %>
  IconCollection<%= classify(collection.name) %>Module,<% } %><% for (let icon of icons) { %>
  Icon<%= classify(icon.name) %>Module,<% } %>
];

@NgModule({
  imports: modules,
  exports: modules,
})
export class IconCollection<%= classify(name) %>Module { }
