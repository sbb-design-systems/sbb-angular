{
  "*1": "scss-bundle requires a config file in the Bazel execroot.",
  "*2": "The config is used in the src/angular:styles_bundle target",
  "bundlerOptions": {
    "logLevel": "error",
    "dedupeGlobs": ["./src/**/*.scss"]
  }
}
