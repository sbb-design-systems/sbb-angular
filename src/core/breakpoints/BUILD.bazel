package(default_visibility = ["//visibility:public"])

load(
    "//tools:defaults.bzl",
    "markdown_to_html",
    "ng_module",
)

ng_module(
    name = "breakpoints",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    module_name = "@sbb-esta/angular-core/breakpoints",
    deps = [
    ],
)

markdown_to_html(
    name = "overview",
    srcs = [":breakpoints.md"],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
