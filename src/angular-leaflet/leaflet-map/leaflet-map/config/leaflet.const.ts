import { LatLng } from 'leaflet';

/** Map default zoom, only used if nothing else is specified. */
export const DEFAULT_ZOOM = 10;
/** Map default center in Berne, only used if nothing else is specified. */
export const DEFAULT_CENTER = new LatLng(46.948094, 7.451563);
