package(default_visibility = ["//visibility:public"])

load(
    "//tools:defaults.bzl",
    "ng_module",
)

ng_module(
    name = "experimental",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    module_name = "@sbb-esta/angular-icons/experimental",
    deps = [
        "//src/icons/experimental/fpl",
        "//src/icons/experimental/kom",
        "//src/icons/experimental/social-media",
    ],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
